//lets create and array with 3 items in it
let favoritesWebsites = ['google.com', 'yahoo.com', 'msn.com']

//lets add another 2 item into the array using .push method. Push will add anohter item to the last possition of the array
favoritesWebsites.push('bbc.com')
favoritesWebsites.push('mozilla.org')

//lets check how many items are in the array an also log them all ouy
console.log(favoritesWebsites.length);
console.log(favoritesWebsites);


//lets remove one item from the array using .pop. 

favoritesWebsites.pop()



console.log(favoritesWebsites.length);
console.log(favoritesWebsites);

